# TODO

* change the default name of / in the site TOC from Overview to something configurable
* code-sample should be nicer to use in the editor. 
  * example: slotting in html should just work without having to escape everything
  * example: should be an easy way for the language injector to detect the slotted language
* components need good bottom-margins
* should be able to set demo to grow to fit content
* need better error messages when data or template can't be parsed/used
* formatting: chop-down only when long
