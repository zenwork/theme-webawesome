# TODO

- [x] change the default name of / in the site TOC from Overview to something configurable
- [x] code-sample should be nicer to use in the editor.
  - [x] example: slotting in html should just work without having to escape everything
  - [x] example: should be an easy way for the language injector to detect the slotted language
- [x] components need good bottom-margins
- [x] should be able to set demo to grow to fit content
- [x] need better error messages when data or template can't be parsed/used
- [x] formatting: chop-down only when long
- [x] the first level of folders in the root for the toc should result in sections of the site and apear as choices in
      the header. The second level of folders should then be the entry point forthe site toc when any section is
      selected from the header.
